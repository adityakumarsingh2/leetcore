import React from 'react';
import { Terminal } from 'lucide-react';

const Navbar = () => {
    return (
        <nav className="navbar">
            <div className="logo">

                <img src="./fulllogo.png" alt="Leetcore logo" className='h-15 mt-' />
            </div>
        </nav>
    );
};

export default Navbar;
